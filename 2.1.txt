In this video, 
I will introduce the idea of limit.
This is not going to be
the rigorous definition YET;
it will be mostly some examples
-- hand-wavy, heuristical --
just to gain some intuition
about what this concept of "limit" means.
And once we are comfortable with that,
in a later video, I will introduce
the formal, rigorous definition.
As our first example,
let's look at this function.
We are calling it f,
and it is defined by this equation:
f(x) = (x^2 - 1) ÷ (x - 1).
This function is defined
for ALL values of x;
the domain is all reals, EXCEPT 1. 
f(1) is undefined.
There is a little simplification 
that I can do:
I can factor the numerator
as (x - 1) ⋅ (x + 1),
and then I can cancel (x - 1)
at the top and bottom,
giving x + 1.
Except this cancellation is ONLY okay
when x is not 1, because when x is 1,
the original function was undefined,
and the new function IS defined.
So to be very precise,
I'm going to give a name
to the original function
and the new function.
I'm going to call f
the original function,
and g the new function.
Then we're saying: 
at 1, they're different;
f(1) is undefined, but g(1) is 2.
EVERYWHERE ELSE,
the two functions ARE the same.
I can use this to sketch the graph
of BOTH of them.
Let's start with g, because it is easier.
g has graph: this line.
Just a line.
And at 1, it's equal to 2.
f, on the other hand,
is exactly the same,
except at 1 it's undefined.
So the graph 
is going to look exactly the same,
but with a HOLE at 1, which I'm going
to exaggerate in the picture.
So it's just a line,
but missing a point or two.
So, looking at this graph, 
f(1) is undefined,
but it's undefined
in kind of a silly way;
it's just because
there's one point missing.
But if I look at ALL the values of x,
in particular, close to 1,
they are all suggesting
that they want f(1) to be 2;
if we had to choose a value for f(1),
it would be 2.
To a certain extent, morally,
f(1) SHOULD be 2.
Even though we "forgot" to define it.
So when this happens,
I'm going to say that 
the limit as x approaches 1 of f(x) is 2.
And I'm going to represent it
with symbols like this:
lim_(x → 1) f(x) = 2.
And that means that,
even though f(1) may be undefined,
as is the case here,
it kind of SHOULD be 2, because
-- this is not a definition,
this is just an idea
and for now it will work --
if x is very close to 1 but NOT 1,
then f(x) is very close to 2,
suggesting that when x = 1,
f(x) should be 2,
even though in this case, it isn't.
For the second example,
I'm going to look at the function 
defined by this equation:
f(x) = (1 - sqrt(1 + x)) ÷ x.
For this function to be defined,
the inside of the root
must be non-negative,
so the domain of this function
is all values greater than or equal to -1
EXCEPT 0, because I'm dividing by x.
So f(0) is undefined.
And I wonder whether f(0) is undefined
like in the previous example:
that if I look at the graph,
it's a nice graph near 0,
except with a hole with a point missing.
I'm going to look at this differently.
I'm going to try to plug in a FEW values,
and see if that is what is happening.
So I just use a calculator
-- this is what f(1) is --
and I'm going to take values of x
CLOSE to 0, closer to 0,
and see what the values of f are.
So this is what f(0.1) is, and 0.01,
and I'll take points closer
and closer to 0.
And I think I see a pattern here:
when x is getting very close to 0,
f(x) is getting very close to -1/2.
So this suggests that, like before,
the graph of this function near 0
looks nice,
except there is a HOLE missing at 0.
If I had to choose a value,
I would choose -1/2 for f(0),
except it is undefined.
So like before, I'm going to say
that if x is very close to 0 but NOT 0,
then f(x) is very close to -1/2,
and again the graph will look nice,
but just with a hole.
I could try to do this algebraically,
like before,
instead of with the table of values.
In this case, a sort
of simplification I can do
is to multiply and divide
by the conjugate of the numerator.
I invite you to pause the video,
multiply it out,
try to simplify it as much as possible,
and then continue watching.
To go quickly through this,
if we continue
manipulating this algebraically,
we can get rid
of the square root at the top,
and get to an expression
where I can simplify by removing an x
from the top and the bottom.
But that simplification
can only be done when x is not 0.
Okay, if that went too quickly,
pause the video and try
to reconstruct these steps
at your own pace.
But the same thing as before
is happening:
this function is equal to ANOTHER one
that is well defined at 0,
EXCEPT when x = 0.
So, the new function
-- I'm calling it g again --
is THIS function.
It's perfectly nice and smooth near zero,
and the value at 0 is -1/2.
And the original function
has the same graph,
except with a HOLE at 0.
So I'm going to say, like before,
that the limit as x approaches 0 of f(x)
is -1/2.
And what that means 
is that even though f(0) is undefined,
if x is very close to 0 but not 0,
then f(x) is very close to -1/2,
suggesting that the value at 0
"should be" -1/2.
To conclude: in summary,
"the limit, as x approaches a, of f(x) 
equals L"
means that if x is very close to a
but NOT a, then f(x) is very close to L.
This just the main idea;
the formal definition will come later.
But first: in the next video,
I'm going to introduce some examples
of functions that DO NOT have a limit,
and explain why.