In this video, I will illustrate
with an example how to resolve
a limit indeterminate form
of type 0 times infinity.
We cannot use
L'Hôpital's Rule directly,
but we can first rewrite the limit,
and then use L'Hôpital's.
This is the example.
I want to compute the limit
as x approaches infinity of
x times (1 – e^(2/x))
I invite you to pause the video,
and try to compute it.
At least, try to understand
what the difficulty is.
Then, continue watching.
This function is a product.
Let's look at the two factors.
The first one is easier.
It has limit infinity.
What about the second?
Well, as x approaches infinity,
2/x will approach 0.
Therefore, e^(2/x) will approach 1.
So, the term inside the brackets
will have limit 1 – 1,
or in other words, 0.
So, I get infinity times 0.
Don't be tempted
to think this is 0.
This is an indeterminate form.
Remember, we are not
actually computing
exactly zero times exactly infinity,
which would make no sense.
As x approaches infinity, I am going
to be multiplying very large numbers
by very small numbers.
The result could be anything.
This is a product.
L'Hôpital's Rule only applies
to quotients.
Luckily, I can rewrite this
as a quotient.
What I did here is
write x as 1/(1/x).
If you simplify this nested fraction,
you get what I started with.
Now that it is a quotient, let's see
what type of indeterminate form it is.
The numerator, as we said before,
has limit 0.
The denominator, when
x approaches infinity,
has limit 0.
Great, so it is a limit indeterminate
form of a type where I can use L'Hôpital.
Perfect. Notice that this trick is
always going to work.
Whenever I have 0 times infinity,
I can rewrite it this way
to get 0/0, or infinity/infinity,
before if a function has limit infinity,
1 over itself will have limit 0,
and vice versa.
Okay. Let's go ahead and
use L'Hôpital's Rule.
I get the limit as x approaches
infinity of...
In the numerator,
I take the derivative,
which is -e^(2/x) times...
chain rule...-2/x^2.
In the denominator, I will simply
get -1/x^2.
Conveniently, I can simplify this
a little bit.
There, this limit is now
easy to compute.
I know the limit of the
exponential part is 1,
so the full limit is -2.
-2 is a number, so this use
of L'Hôpital was legal,
and the original limit is -2.