In this video, I will explain the
definition of sequence;
and I will present various common
conventions (and pieces of notation),
that we use to describe sequences.
The definition of
sequence is short.
A sequence is just a function
with a domain of the natural numbers.
(So it's a particular type of function.)
This is what the graph of an arbitrary
function might look like.
The graph of a sequence
may look like this:
the domain is only
the natural numbers (ℕ),
so I just get this bunch
of disconnected dots.
This is a reason why we normally don't
represent the graph of a sequence.
It doesn't give us as much information as
the graph of other functions.
While a sequence is a
particular type of function,
there is the common convention
of distinguishing...
...between two types of functions.
Sometimes we agree that
if we use the term "function",
we mean: a function with
domain of an interval,
or perhaps a union of intervals.
(this is a little vague)
Whereas, when we say "sequence", we mean:
a function with domain of the naturals;
and I'll relax that a little bit
at the end of the video.
Ultimately they are all functions,
but sometimes we agree that "function"
will mean ONLY the first type.
We have a further convention,
of having some meaning given
to the choice of variable.
We will often, for a function as in the
first type, use "x" as the variable;
whereas, we would use "n"
as the variable for a sequence.
There's no good reason for this,
you could use "n" to be the variable of
a function and "x" for a sequence,
but we commonly agree on this,
so that it is implied that
if the variable is called "n",
then it only takes natural values.
If I use x as the name of
the variable for a function,
and the function is called f,
then the value of the function at x
is f(x) (in parentheses);
but for the value of the sequence at n,
we'll often write aₙ as a sub index,
if a is the name of the sequence.
Again, there is no good reason for this,
we could use indices for functions
and parentheses for sequences,
but we often agree to do it this way.
So for example, I could define the function
f by the equation f(x) = 3/(x + 1),
and I could define a sequence "a",
by the equation aₙ = 3/(n + 1).
It's the same equation,
but in the second case I'm emphasizing
that the domain is only natural numbers.
n only takes natural values.
In the second case,
since n only takes natural values,
I can order all the terms and I can say
that the first few terms will be:
3/1, 3/2, 3/3 and so on,
which I could not do with the function.
In this notation, it is also common to
write those terms in curly braces,
even though it's perhaps a bit unfortunate,
since a sequence is NOT a set.
The set could be the RANGE of the sequence,
but the sequence also includes saying:
what is the first term, what's the second
term, and what's the third term,
which a set could not.
Nevertheless, this notation with
curly braces is also common.
That's because there are various
ways to define a sequence.
We could describe a sequence
with an equation.
(Perhaps that's the most common way.)
Here is an example:
in this case aₙ = 2ⁿ⋅n!/(n + 1),
this is definitely a sequence,
because the factorial (n!) is only
defined for natural numbers.
But we can also define a sequence
by giving the first few values.
For example,
I could define a sequence with first few
values: 1, 2, 4, 8, 16, and so on...
But be careful every time we define a
sequence with the first few values,
because when I wrote that perhaps you
were thinking of the general term 2ⁿ,
but I wasn't,
I meant 2ⁿ plus this polynomial.
And you can check it, it actually works.
The lesson here is that
the first few terms of a sequence never
FULLY determine what the sequence is.
So the convention is,
that if by writing the first few terms
it's very clear what you meant,
then sometimes we do that, but
otherwise we should write the equation.
So for example, in this case if I
actually meant this complicated equation,
I should not define this sequence
just by the first few values.
A fair way to describe a sequence
is with words with an explanation.
For example, I could describe the
sequence of primes,
where pₙ is the n-th prime.
This is a perfectly
well-defined sequence.
We know what the terms are, the first
few ones are 2, 3, 5, 7, 11...
And the reason we do
it this way (with words),
rather than with an equation is because
there is not a nice equation for it,
but the description with words
is perfectly valid.
And there is yet a fourth
common way to describe a sequence...
...that is very specific for sequences,
and that is via a recurrence relation.
Perhaps the best known example
of this is the Fibonacci sequence,
that we normally denote by Fₙ,
which is defined by the following:
I say that the zeroth term is 1,
the first term is 1,
and after that every term
is the sum of the previous two.
And yes writing this fully
defines what the sequence is,
because for example. F₂ (the next term),
will be the sum of F₁ and F₀,
which is 1 + 1, which is 2;
and F₃ (the next term), will be the sum
of the second and first,
which is 3, and so on...
So just by giving this rule, I can
calculate any term in the sequence.
Why would we describe the sequence this
way rather than with an equation?
Well, for two reasons:
perhaps there isn't an equation,
or perhaps there is,
but this description is clearer.
There is actually an equation for the
general term of the Fibonacci sequence...
...but it's not very enlightening,
whereas this property
defines it much better.
An important note on notation.
Perhaps the most complete notation for a
sequence may be something like this:
I write aₙ between curly braces,
and I say that n goes from 0 to ∞.
That's how I will read those indices.
That tells me what the variable is,
and reminds me that it starts at 0,
and it keeps on going forever.
Sometimes you will see other notations,
which sometimes are an abuse
of notation, but we use them anyway.
For example, this is another
way to express the same thing.
Or sometimes I may just write that,
without specifying that it starts at 0.
Or even that, where I'm just
emphasizing what the variable is.
(I assume the rest is understood.)
The other way around:
perhaps I use this notation,
where I'm not specifying that n is the
variable, (I assume that's understood)
and I specify the rest.
Or maybe I'm very lazy
and I just write that.
All of those may be different
notations for sequences,
and it's okay to use them
when they are unambiguous,
and it's very clear what we mean.
Some books may use parentheses
instead of curly braces,
which may be a good idea
because a sequence is not a set,
and the curly braces may confuse us.
So all of those are valid pieces of
notation when there is no ambiguity.
But be careful, if there is
ambiguity we should be very precise.
For example, if I want to describe this
sequence: {n/k} when k goes from 2 to ∞,
I really should write everything,
because in this case there are
two variables (n and k),
and I have to say which one I mean as
the variable for the sequence.
(in this case k)
So for the purpose of the sequence,
n is a constant.
And also, in this example, I want
the variable to start at 2 not at 0,
so this is the sequence
that I actually mean.
This final example leads me to conclude
to change the original definition,
because this function doesn't have the
natural numbers as it's domain.
(It's a bit smaller.)
So I'm going to change
the original definition.
I said initially that
a sequence was a function...
...with a domain of the natural numbers;
but instead, I'm going to say that the
domain is something like this:
the set of integer values
≥ some fixed integer n₀,
which is to say that I accept a sequence
with a domain of the natural numbers,
but also if they start at 1 instead of 0,
or at 3, or at 7, or perhaps even at -1.
So I'm going to accept all of those,
(relax my definition)
and say any of those is a sequence.
For example, the sequence 1/ln(n),
where n is the variable,
is naturally defined
with domain starting at 2,
because I can not divide by ln(1),
since that would be 0.